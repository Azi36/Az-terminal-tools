# 真机验证清单

这里只记**本地跑不了、必须连一台真服务器才能确认**的事。
单元测试和类型检查该进 CI 的进 CI，不往这儿抄。

开发机上没有 WSL / Docker / sshd，dev 实例读的又是真实的连接数据，
所以下面这些一律攒着，等有一台可以随便折腾的机器时一次跑完。

## 怎么跑

1. `npm run tauri dev`
2. 连一台**可以随便折腾的** Linux（别拿生产机试 kill 和 systemctl 那几条）
3. 照着下面一条条走，能过的打勾，过不了的把实际输出贴在那一条下面

最好凑齐三种机器，它们各自会踩到不同的坑：

| 机器 | 为什么要试它 |
| --- | --- |
| 主流发行版（Ubuntu / Debian / CentOS） | 基准，`ps`、`df`、`ss` 都是 GNU 那一套 |
| busybox 系（Alpine / 路由器 / 精简容器） | `ps` 的列不一样、`du -d` 认不认、可能没有 `who` |
| 非 Linux（macOS / FreeBSD，有就试） | 该明说「看不了」，不该报错也不该画半真半假的图 |

---

## 状态 · 概览

- [ ] **能出数**。连上点「状态」，第一轮之后 CPU / 内存 / 网络三格都有数字，
      不是一直卡在「正在取第一轮数据……」。
- [ ] **CPU 占用对得上**。服务器上开一个 `yes > /dev/null`，面板里的 CPU 该涨
      约 `100/核数` 个百分点；`Ctrl+C` 之后掉回去。
- [ ] **核数对**。「N 核」跟 `nproc` 一致；下面的小柱子是 N 根。
      跑单核满载时应该只有一根柱子顶到头 —— 这一条是这个视图存在的理由。
- [ ] **内存对得上**。跟 `free -m` 的 `available` 那一列对一下，
      面板显示的「已用」应该是 `total - available`，不是 `used`。
- [ ] **交换区**。有 swap 的机器上要显示百分比；`swapoff -a` 的机器上这一行该消失。
- [ ] **负载**。跟 `uptime` 的三个数一致。
- [ ] **网络速率**。`curl` 拉个大文件的时候 ↓ 该冲上去；空闲时接近 0。
      回环口的流量不该算进去（`iperf3 -c 127.0.0.1` 时曲线应该基本不动）。
- [ ] **磁盘只列真盘**。`tmpfs`、`devtmpfs`、`overlay`、`none` 不该出现；
      跟 `df -Pk` 里设备名以 `/` 开头的那些条目一一对应。
- [ ] **挂载点带空格**能显示全（`mount --bind` 造一个 `/mnt/a b` 试）。
- [ ] **刷新间隔**。切到 10 秒之后，`ss` 或抓包确认真的变慢了，不是前端丢弃。
- [ ] **切走就停**。切到终端页待着，服务器上 `ps aux | grep -c 'proc/stat'`
      不再增长；切回来又接上。
- [ ] **断开重连**不留假尖峰。断开再连，第一轮 CPU 显示「—」而不是一个离谱的数。
- [ ] **通道不堆积**。挂着状态页跑十分钟，`ss -tn | grep :22 | wc -l` 不涨；
      服务器 `MaxSessions` 没被打满（这是「一轮只开一个通道 + 显式 close」要保的）。

### 风险最高的两条

- [ ] **busybox 上 `who` 可能没有** → 「登录会话」显示 0 而不是报错。
- [ ] **老内核（< 3.14）没有 `MemAvailable`** → 该退回 `free + buffers + cached`，
      不该显示 0%。找不到这么老的机器就手动把 `/proc/meminfo` 那一行注掉验证解析。

## 状态 · 进程

- [ ] **列出来了**，条数跟 `ps -e | wc -l` 大致相当（面板截 400 行）。
- [ ] **命令行完整**。找一条参数带空格的（比如 `java -jar x.jar --a=b c`），
      确认没被切碎、也没被截断。**这条在 busybox 上最容易挂**，因为 `ps` 的列宽不同。
- [ ] **中文路径**。在 GBK 的机器上跑一个路径带中文的进程，切到 GBK 编码后不乱码。
- [ ] **排序**。「按 CPU」「按内存」切换后顺序真的变了。
- [ ] **搜索**。按命令、用户名、PID 三种都能搜到。
- [ ] **kill 自己的进程**：起一个 `sleep 999`，发 TERM，它没了，刷新后列表里不在了。
- [ ] **kill 别人的进程**（不加 sudo）应该失败，且错误信息说得清是权限问题。
- [ ] **sudo kill**：在配了免密 sudo 的机器上勾「用 sudo 发」能成。
- [ ] **要输密码的 sudo**：不该卡住不动，应该当场返回「去终端页手动 kill」。
      —— 这条是 `sudo -n` 那个 `-n` 的全部意义，务必验。
- [ ] **PID 1 拦住了**：手工调 `stats_kill` 传 pid=1 应该被拒（界面上不容易点到）。

## 状态 · 端口与服务

- [ ] **端口列出来了**，跟服务器上 `ss -lntup` 的结果一一对应（条数、端口号）。
- [ ] **`ss` 缺席时退回 `netstat`**。把 `ss` 临时改名（`mv /usr/bin/ss /usr/bin/ss.bak`）
      再刷新，标题旁边的小标签应该从「来自 ss」变成「来自 netstat」，表格照常。
      **这条最值得测**，两个工具的列数不一样，udp 行还少一个 State 列。
- [ ] **IPv6**。`[::]:22` 这类地址栏里应该显示 `::`，不带方括号。
- [ ] **udp 行**（比如 `:68` 的 dhclient）能正常出现，不被当成坏行丢掉。
- [ ] **「对外 / 仅本机」判断对**。绑 `0.0.0.0` / `::` 的标「对外」（琥珀色），
      绑 `127.0.0.1` 的标「仅本机」。拿 `python3 -m http.server --bind 127.0.0.1`
      和不带 `--bind` 的各起一个对比。
- [ ] **非 root 连的时候**：进程列应该是空的，并且上面出现「一个进程名都没读到」那句话，
      而不是让人以为这些端口没人占。

- [ ] **服务列出来了**，条数跟 `systemctl list-units --type=service --all | wc -l` 相当。
- [ ] **排序**：failed 的排在最前面。造一个必挂的 unit 验证。
- [ ] **按钮跟状态匹配**：active 的行给「重启 / 重载配置 / 停止」，
      inactive / failed 的行只给「启动」。
- [ ] **真能动**：拿一个无关紧要的服务（`cron`、`atd`）停一次、起一次，
      `systemctl status` 对得上，面板刷新后状态也变了。
- [ ] **免密 sudo 的机器**上勾着 sudo 能成。
- [ ] **要输密码的 sudo**：不该卡住，该当场返回「去终端页手动做」。
- [ ] **polkit 拦截**：非 root 且没配 sudo 时，systemctl 会要交互式认证，
      应该识别出来并给出那句提示，而不是干等到超时。
- [ ] **不用 systemd 的机器**（Alpine 的 OpenRC、老 CentOS 6）：
      「服务」那一块该显示「这台机器可能不用 systemd」，
      而**端口那一块要照常能看** —— 两边是分开成败的。
- [ ] **`systemctl` 不会去开 pager**。这是加 `--no-pager` 的原因；
      去掉它会让命令挂到超时。真机上确认一次这条命令是秒回的。

## 状态 · 容器

- [ ] **列得出来**，跟 `docker ps -a` 一一对应（在跑的和停了的都要有）。
- [ ] **镜像名带私有仓库前缀**（`registry.cn-*.aliyuncs.com/...`）时不被截断成看不懂。
- [ ] **端口那一列**里有逗号和空格（`0.0.0.0:8080->80/tcp, :::8080->80/tcp`），
      要完整显示 —— 这是按 tab 切而不是按空格切的理由。
- [ ] **没装 docker 的机器**：给「这台机器上没有 docker」，不是一堆英文。
- [ ] **装了但没权限**（普通用户没进 docker 组）：应该提示勾「用 sudo」或者加进 docker 组。
- [ ] **守护进程没跑**：给「docker 在，但守护进程没跑起来」。
- [ ] **一个容器都没有**时是空态提示，不是报错。
- [ ] **日志**：点开能看到内容，只有最后 300 行。
- [ ] **启动 / 停止 / 重启**真的生效，刷新后状态跟着变。
- [ ] **「进去」**：点了之后跳到终端页，并且真的进了容器（`hostname` 是容器 id）。
      **这条要确认命令是在终端里跑的**，不是在面板那条没有 PTY 的通道里。
- [ ] **容器 id 安全**：面板只拿 id（十六进制）不拿名字。手工构造一个带分号的 id
      调 `stats_docker_do`，应该被拒。

## 状态 · 端口一键转发

- [ ] **转发建得起来**。在端口页找一个「仅本机」的（比如 6379），点「转发到本地」，
      确认本地口，之后在本机 `redis-cli -p <本地口>` 能连上。
- [ ] **默认本地口**：远端 ≥1024 时同号；小于 1024 时建议 +10000（80 → 10080）。
- [ ] **建完自动跳到隧道页**，那一行在，而且是开着的。
- [ ] **本地口被占用**时给出能看懂的错误，不是静默失败。
- [ ] **「在浏览器里打开」**只在本地转发且开着时出现，点了能打开对应的
      `http://127.0.0.1:<口>`。

## 状态 · 网络工具

- [ ] **ping** 一个通的和一个不通的目标，四个包之后都要自己停下来，
      不该一直转（这是 `-c 4 -W 2` 要保的）。
- [ ] **端口探测**：探一个开着的和一个关着的端口，结论要说得明白。
      在**没有 nc** 的机器上也要能出结果（退回 bash 的 /dev/tcp）。
- [ ] **DNS 解析**：解一个内网域名。没装 dig / host 的机器上要退回 getent，
      而不是报「命令找不到」。
- [ ] **路由追踪**：没装 traceroute 的机器上退回 tracepath；两个都没有时明说。
- [ ] **目标里带特殊字符**：填 `a.com; id`、`$(whoami)`、`a.com && curl x`，
      三个都必须被拒（「目标只能是主机名或 IP」），**绝不能执行**。这条是安全项。

## 从别的工具导入

单元测试是照着这三家**文档里的格式**写的夹具。真实导出文件的版本差异只能拿真文件验，
所以这一节必须用真的导出文件跑一遍。

- [ ] **Xshell**：从 Xshell 里导出（或者直接拿 `Documents\NetSarang Computer\...\Sessions`
      底下的 `.xsh`），一次选好几个，主机 / 端口 / 用户名都对得上。
- [ ] **中文会话名**的 `.xsh` 不乱码（中文 Windows 上这些文件常常是 GBK）。
- [ ] **PuTTY**：`regedit` 导出 `HKCU\Software\SimonTatham\PuTTY\Sessions`，
      导出来的 `.reg` 是 **UTF-16LE**，要能正常读；端口那一列是十六进制 dword，
      2222 端口应该显示成 2222 而不是 8738。
- [ ] **PuTTY 最后一个会话**也要被认出来（文件末尾没有空行收尾）。
- [ ] **Termius / 其它 JSON**：拿真的导出文件试，认出来的条数对不对。
- [ ] **一次选多个文件**，同一台机器出现在两个文件里时只进一条。
- [ ] **已经在库里的机器**不重复添加，提示说清楚。
- [ ] **选了个不相干的文件**（图片、几十 MB 的日志）不该崩，应该跳过。
- [ ] **认不出东西**时给的是「换个导出格式试试」这类能照着做的话。

## 命令补全建议

- [ ] 敲一条命令回车，再敲它的前几个字符，底下要浮出建议；按 `→` 补全，回车能执行。
- [ ] **点那条建议**也能补全。
- [ ] **前两个字符不提示**（太短的时候提示只会碍事）。
- [ ] **退格**之后建议跟着变短 / 消失。
- [ ] **跟丢之后不提示**，这几种都要验：
      按 `↑` 翻历史、按 `Ctrl+U` 清行、按 `Tab` 让 shell 补全、粘贴一段文本。
      跟丢之后**直到下一次回车**都不该再出现建议。
- [ ] **行尾按 `→` 没有建议时**，行为跟原来一样（在 shell 里是空操作），
      不该被吞掉或者出怪事。
- [ ] **行中间按 `→`**（先按 `←` 移到中间）：那时候已经跟丢了，所以不该有建议，
      `→` 要正常移动光标。
- [ ] **vim / top 这类全屏程序里**不该冒出建议来捣乱。
- [ ] **换一条会话**（断开重连、开新标签）历史是空的 —— 它不落盘。
- [ ] 敲一条带密码的命令（`mysql -pxxxx`），**关掉应用重开**，
      它不该再出现在建议里，`localStorage` 里也搜不到。

## 网盘同步（WebDAV）

拿一个真的 WebDAV 账号跑（坚果云的「应用密码」最好使）。

- [ ] **上传**：填好地址 / 用户名 / 密码，设个口令上传，去网盘里看那个文件 ——
      应该是一段 `"enc": "argon2id-xchacha20poly1305"` 的 JSON，
      **里面搜不到任何主机名**。
- [ ] **拉下来**：在另一台机器（或者先清掉本地数据）填同样的配置，用同一个口令拉，
      连接 / 片段 / 备忘 / 收藏都回来了。
- [ ] **第一次拉**（服务器上还没那个文件）：给「服务器上还没有这个文件」，不是 404 报错。
- [ ] **口令输错**：给「口令不对，或者这个文件被改过」，本地数据一个字节都没变。
- [ ] **密码错**：给「账号或密码不对（401）」。
- [ ] **地址指到目录**（结尾是 `/`）：在点之前就被拦下，提示要指到一个文件。
- [ ] **上级目录不存在**：给「这个路径不存在（404）—— 上级目录得先建好」。
- [ ] **地址填成一个普通网页**：GET 回来是 HTML，应该在解密那一步失败并说得清，
      而不是把一堆 HTML 当配置导进来。
- [ ] **密码存在钥匙串里**：翻一遍 `localStorage` 和导出的备份 JSON，
      **不该出现网盘密码**。
- [ ] 重开应用后地址和用户名还在，密码不用重填。

## 全局热键

- [ ] 设一个热键，**把窗口切到后台**（点别的程序），按热键要能把它叫到最前。
- [ ] **再按一下收回去**（隐藏），第三下再出来。
- [ ] **窗口最小化时**按热键要能还原并拿到焦点 —— 最小化的窗口 isVisible 仍是 true，
      只看那一个的话会「按一下没反应，再按一下才出来」。
- [ ] **选一个被占用的组合**（比如某截图软件占着的），应该在设置里出现红字说注册不了，
      而不是静默失败让人以为设好了。
- [ ] **改成「不用」**之后，原来那个组合不再抢系统的键（去别的软件里按一下确认）。
- [ ] 重开应用后热键还在。

## 状态 · 磁盘占用

- [ ] **量得出来**。`/var` 点「量一下」，各子目录的数跟 `du -kx -d 1 /var` 一致。
- [ ] **下钻**。点进去能一层层往下，「上一级」能回来，`/` 的「上一级」是灰的。
- [ ] **不跨文件系统**。在 `/` 上量，不该把 `/proc`、`/sys`、网络挂载的量算进来，
      也不该卡住几分钟。
- [ ] **没权限的目录**：用普通用户量 `/root` 的上层，该出现「N 个子目录没读进去，
      数偏小」的琥珀提示，而不是直接报错或者悄悄少算。
- [ ] **「这一层的文件」**那一行的数 = 总量 − 各子目录之和，跟
      `du -kx --max-depth=0` 减一下能对上。
- [ ] **busybox 的 `du`** 认不认 `-d 1`。不认的话现在会报「量不出来」，
      要确认错误信息说得清，必要时换成 `--max-depth`。
- [ ] **超时**。找一个巨大的目录，超过 90 秒该给出「跑太久」而不是一直转。
- [ ] **带空格 / 引号的路径**。`mkdir "/tmp/a b"` 和 `mkdir "/tmp/x'y"` 各量一次，
      不该报错、更不该执行成两条命令。

## 日志查看器

先在服务器上造一个够大的：`yes "$(date) some log line" | head -c 3G > /tmp/big.log`

- [ ] **开得动**。文件面板右键 `/tmp/big.log` →「日志查看器」，秒开，
      直接停在文件末尾。**开一个 3GB 的文件不该有任何等待感** —— 有的话说明
      它在读整个文件，而不是只读末尾那 256 KB。
- [ ] **大文件排在前面**。超过 2MB 的远程文件，右键菜单里「日志查看器」应该在
      「编辑」上面。
- [ ] **翻页**。开头 / 上一屏 / 下一屏 / 末尾四个按钮都对得上，
      百分比跟着变，到头到尾时对应的按钮变灰。
- [ ] **行不被切断**。翻到中间时，第一行和最后一行都是完整的一行，
      不该看到半截行（除非顶上明说「有一行长得超过一屏」）。
- [ ] **超长单行**。造一个 `python3 -c "print('x'*400000)" > /tmp/oneline.log`，
      打开它不该是一片空白，应该显示内容并提示被切开了。
- [ ] **搜索**。搜一个存在的词，命中列表出来，点一条能跳过去，
      跳过去之后那一屏里确实有这个词。
- [ ] **搜索是在服务器上跑的**。搜的时候看服务器上有没有 `grep` 进程，
      同时看本机网卡 —— 应该只回来几十 KB 的命中行，不是整个文件。
- [ ] **正则 / 忽略大小写**两个开关都真的生效。
- [ ] **模式里带特殊字符**：搜 `$(whoami)`、`` `id` ``、`'; ls;` 三个，
      应该老老实实当字面量搜（搜不到就是搜不到），**绝不能真的执行**。
      这条是安全项，务必验。
- [ ] **搜不到东西**时是「没找到」，不是报错。
- [ ] **没权限的文件**（`/var/log/secure`）用普通用户搜，应该给出权限相关的错误，
      而不是「没找到」。
- [ ] **跟随**。`while true; do date >> /tmp/big.log; sleep 1; done`，
      停在末尾时新行应该自己接上、并且自动滚到底。
- [ ] **翻到中间时不跟随**。上一条跑着的同时翻到文件中间，**不该被拽回末尾**。
- [ ] **换编码不丢位置**。翻到中间，把编码从 UTF-8 换成 GBK 再换回来，
      应该还停在原地，不该弹回文件末尾。
- [ ] **GBK 日志**。造一个 GBK 的日志（`iconv -f utf-8 -t gbk`），
      默认按连接的编码解；解错时右上角出现「编码可能不对」。
- [ ] **断线**。看着日志的时候把 SSH 断掉，应该提示「只能看已经取回来的这一屏」，
      不该反复报错或者空白。
- [ ] **关掉会话标签**，挂在它下面的日志标签也一起关掉。

## 登录后执行

- [ ] **会跑**。填 `cd /tmp` + `pwd`，连上后终端里能看到命令和 `/tmp`。
- [ ] **不跟 motd 挤在一起**。有登录 banner 的机器上，命令应该出现在提示符之后。
- [ ] **多行**按顺序一条条执行。
- [ ] **掉线自动重连之后会再跑一遍**（拔网线或 `iptables` 掐一下）。
- [ ] **手动断开后不再跑**。断开的瞬间那个 400ms 定时器要撤掉，
      不该敲进下一条会话。
- [ ] **GBK 机器上**命令里的中文路径不乱码。
- [ ] **导出 → 导入**能把这一栏还原回来。

## 命令面板 / 同步输入 / 分屏

这三样在本机就能验个大概，但下面几条得连上真机才算数。

- [ ] **Ctrl+Shift+P 在终端里按得动**。焦点在终端画布上时按它，面板要弹出来，
      而且这个组合**不能有任何字符被送到服务器**（在 shell 里按完看命令行是不是干净的）。
- [ ] **Ctrl+K 还是删到行尾**、**Ctrl+P 还是上一条历史** —— 这两个键我们没抢。
- [ ] 面板里选一条片段，能插到当前终端；没有活着的会话时片段那一组不出现。

- [ ] **同步输入**。开两个标签连两台机器，两边都点「同步」，
      在其中一台里敲 `hostname` 回车，两台都要执行，各自回显自己的主机名。
- [ ] **粘贴也走同步**（右键粘贴 / Ctrl+Shift+V）。
- [ ] **回显不会互灌**：两台的输出各是各的，不该出现 A 的输出跑到 B 的屏幕上。
- [ ] **只有一台在组里时不广播**，按钮是「就绪」的样子而不是警告色。
- [ ] **关掉其中一个标签**，另一台的「同步 N」数字要跟着变，不该还显示 2。
- [ ] **重开应用后同步组是空的**（故意不落盘）。

- [ ] **分屏**。右键标签「在右边分屏打开」，两块并排；两块的终端都能正常收发。
- [ ] **焦点**。点哪半哪半得焦点（边框亮起来），此时 Del / F2 这类文件面板快捷键
      只对有焦点的那半生效，**往窗口里拖文件也只落到有焦点的那半**。
      这条是分焦点和可见两个概念的全部理由，务必验。
- [ ] **状态页在分屏里照常刷新**（没焦点也刷）—— 左边看状态右边敲命令是主要用法。
- [ ] **拖分隔线**能调宽窄，双击回到一半一半；改成上下分之后同样能拖。
- [ ] **点分屏那一半对应的标签**，两块对调，不该出现分屏那边空掉。
- [ ] **关掉分屏里的那个标签**，自动退回单屏。
- [ ] 分屏时终端尺寸要重新算（PTY 的 cols/rows 跟着变）——
      在窄的那半里跑 `top`，边框不该错位。

## 持久会话（tmux / screen）

- [ ] **选 tmux 之后连上，人在 tmux 里**。`echo $TMUX` 非空，或者 `Ctrl+B d` 能 detach。
- [ ] **断线不丢活儿**。在里面跑 `top`，把网线拔了 / `iptables` 掐一下，
      等自动重连接回来，`top` 还在跑。**这是这个功能的全部意义，必须验。**
- [ ] **接回的是同一个而不是新开一个**。重连之后在服务器上 `tmux ls`，
      只应该有一个 `az-xxxxxxxx`，不该越攒越多。
- [ ] **改窗口大小**，tmux 里的 pane 跟着变，`top` 的边框不错位。
- [ ] **没装 tmux 的机器**（先 `mv /usr/bin/tmux`）：应该打印
      「[AzTerm] 这台机器上没有 tmux，退回普通 shell」并给出一个能用的 shell，
      不该是一个死终端。
- [ ] **screen 那条路**同样验一遍（`screen -xRR`）。
- [ ] **登录后执行**和持久会话一起用时，命令要敲进 tmux 里那个 shell。
- [ ] **同一条连接开两个标签**：两个都会 attach 到同一个 tmux 会话，
      屏幕内容是同步的（这是 tmux 的正常行为，确认没报错就行）。
- [ ] **会话名安全**：手工把配置里的 persist 改成带分号的名字（改 localStorage），
      引擎应该拒绝并报「名字不合规矩」，**不能拼进命令行执行**。

## 加密导出 / 导入

这一块不用连服务器，但**必须真跑一遍完整往返**，不能只看单元测试。

- [ ] **加密导出**。设个口令导出，用文本编辑器打开那个文件：
      应该是一个带 `"enc": "argon2id-xchacha20poly1305"` 的 JSON 信封，
      **里面搜不到任何一个主机名或用户名**。
- [ ] **同一份内容导两次，两个文件的密文不一样**（盐和 nonce 每次新取）。
- [ ] **导入回来**：换一台机器（或者先把本地数据清掉）导入，
      连接、片段、备忘、收藏都回来了，跟导出前一致。
- [ ] **口令输错**：给出「口令不对，或者这个文件被改过」，不该崩、也不该导入一半。
- [ ] **文件被改过**：用编辑器把 `data` 里改一个字符再导入，同样要被拒。
- [ ] **明文备份照常能导**（老版本导出的文件不能因为这次改动就打不开了）。
- [ ] **口令框是遮起来的**，不会被浏览器/系统的密码管理器记住。
- [ ] 口令少于 8 个字符时导出被拒。

## 非 Linux

- [ ] macOS / FreeBSD 上点「状态」，应该显示「这台是 Darwin，状态面板还看不了它」，
      而不是一屏 0、也不是红色报错。

---

## 记一笔

跑完之后，把过不了的那几条连同真实输出一起留在这儿，别只写「挂了」——
`ps` 和 `du` 在各家系统上的差异全靠这些原始输出才能改对。
