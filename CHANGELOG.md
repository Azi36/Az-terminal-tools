# 更新日志

这一版改了什么，都记在这儿。

发版时把对应小节的内容抄进 GitHub Release 的说明里 —— `notify.yml` 会把 Release
正文推给版本接口，应用「设置 → 关于」里显示的就是这段话。所以写给用户看，别写
内部术语。

版本号按 [语义化版本](https://semver.org/lang/zh-CN/)：能用的功能变化进次版本号，
只修问题进修订号。

## 未发布

### 新增

- **git 面板重做**。原来是九个按钮加一张平铺的改动列表，现在按实际干活的顺序摆：
  顶上是分支（点开能筛、能直接新建一条），下面是拉取 / 推送 / 抓取三个按钮，
  落后和领先的条数直接做成角标标在按钮上；再下面是提交框，Ctrl+Enter 就提交，
  一个文件都没暂存时会先替你 `git add -A`，省得 git 甩一句「nothing added」白跑一趟。
  改动按**冲突 / 已暂存 / 改动 / 没跟踪**分开，空的那组不占地方；
  每一行鼠标移上去有暂存、取消暂存、扔掉改动，点文件名看它自己的 diff。
  扔改动和删没跟踪的文件会先问一句 —— 这两件事没有后悔药。
  提交列表默认六条可展开，stash 单列一条，不常用的命令收进「其它命令」。
  跟以前一样，按钮都只是把命令送进左边的终端，输出你自己看，面板不在背地里跑 git。
- **更新终于有进度了**。原来点了「检查更新」只有一句不动的「下载中……」，
  几十兆的包下着，分不出是在下还是卡死了。现在是一条四步的进度：检查 → 下载 →
  安装 → 重启生效，下载那一步显示已下载多少、总共多大、多快、还剩多久。
  服务器不报总大小时走不确定进度条，不编一个假的百分比糊弄。
  「安装」这一步单列出来，因为校验签名和写盘是真的要等一会儿。

### 体验

- **全局补了动效**：切标签、开弹窗、开始页的卡片、面板滑入、分组展开、按钮按下去
  都有了过渡，连着的会话那颗绿点会慢慢呼吸。都只动透明度和位移，不碰宽高，
  终端和文件列表那种一屏几千个节点的地方不会掉帧。
  系统里关了动画效果的，一条都不放。

### 修复

- **终端最后一行不再被下边缘切掉一半**。系统缩放不是整数倍的时候（Windows 常见的
  125% / 150%），终端算出来的行数会比实际能显示的多一行，最下面那行只露出小半截，
  刚敲的命令和提示符看不全。现在按真正可见的那块重新收一遍行列数。
  SSH 会话和本地终端用的是同一个终端组件，两边一起好了。
- **git 面板不再停在上一个仓库**。cd 得快的时候，先发出去的那次查询后回来，
  会把新目录的结果盖掉，面板就一直显示着上一个仓库的分支和改动。
- git 面板送进终端的路径和提交说明现在按 shell 的规矩加引号，
  里头带空格、`$`、反引号都不会被 shell 吃掉或者拆开。

## 0.8.0 —— 2026-09-08

### 新增

- **数据库（第一步）**。侧栏「数据库」那一栏终于不是占位了：MySQL、PostgreSQL、Redis 三种连接，
  密码跟 SSH 一样存系统钥匙串、不进配置文件和备份。开一个标签就是控制台：
  SQL 那两家是「库表树 + 编辑器（Ctrl+Enter 执行）+ 结果表」，点一张表就是 `SELECT * … LIMIT 100`，
  结果默认最多 500 行、可调到 5000；Redis 是「按模式扫 key（带类型和 TTL）+ 命令行（↑↓ 翻历史）+ key 内容预览」。
  库只对内网开放的话，先到那台服务器的隧道页开一条本地转发，这儿填 127.0.0.1 —— 自动开隧道留到下一步。
- **本地终端**。侧栏「本机终端」或 Ctrl+Shift+L 在本机开一个 shell 标签，能开好几个，
  Windows 上自动挑 pwsh（没有就是系统自带的 PowerShell），macOS / Linux 用 $SHELL，设置里可以指定。
  终端右边挂着一块 **git 面板**：cd 进仓库就自动出现，显示分支、领先落后、改动列表、最近提交，
  一排按钮（状态 / 拉取 / 推送 / 抓取 / 差异 / 日志 / 暂存 / stash）和切分支的下拉，
  点的都是把命令送进左边的终端，输出在那儿看，不藏着。指令库和命令面板里的片段也能发进本地终端。
  目录跟踪靠 shell 出提示符时上报（PowerShell 和 bash 会，zsh 暂时不会，面板上可以手填）。
- **服务器状态页**。会话标签里多了一页「状态」，连上就能看这台机器的 CPU、内存、
  交换区、负载、网卡吞吐和各个挂载点的用量，都是实时的，还带一条两分钟的走势线。
  CPU 那一格下面按核画了小柱子，一眼分得出是整机在忙还是某一个核在死磕。
  刷新间隔可以在 1 / 2 / 5 / 10 秒之间选。
- **进程管理**。同一页里能按 CPU 或内存排序看进程，按命令、用户、PID 搜，
  也能直接结束某个进程 —— 信号在 TERM / INT / HUP / KILL 里选，
  需要的话勾上「用 sudo 发」（只在配了免密 sudo 的机器上管用）。
- **端口与服务**。同一页里能看这台机器上所有监听端口：端口号、协议、绑在哪个地址、
  被哪个进程占着，并且直接标出哪些是「对外」（绑在 `0.0.0.0` / `::` 上）、
  哪些只有本机能连。下面接着列 systemd 服务，failed 的排最前，能搜、能只看没在跑的，
  也能直接启动 / 停止 / 重启 / 重载配置。
- **磁盘占用分析**。填一个目录点「量一下」，列出各个子目录各占多少、占这一层的
  百分之几，点进去还能接着往下钻。不跨文件系统，所以不会顺着 `/proc`
  和网络挂载爬出去。因为大目录要跑一会儿，它不会自动跑、也不轮询。
- **大日志查看器**。`/var/log` 底下几百 MB 到几个 GB 的文件，内置编辑器 2MB 就拦下了，
  下载下来又要等半天。现在在文件面板里右键 →「日志查看器」，秒开，直接停在文件末尾，
  能一屏一屏往前翻，也能开「跟随」看新写进来的行。
  **搜索是让服务器自己 grep 的** —— 几个 GB 的文件在那边扫一遍是几秒钟，
  扒回本地再搜是几分钟；命中列表点一下就跳过去。
- **登录后自动执行命令**。连接配置里新增一栏，一行一条，连上就像自己敲的那样
  进终端，掉线自动重连之后也会再跑一遍。

- **命令面板（Ctrl+Shift+P）**。一个搜索框把所有入口收拢：连接、开着的标签、
  当前标签里的几页、指令片段、备忘，还有新建连接 / 设置 / 换主题这些动作。
  敲几个字回车就到。用 Ctrl+Shift+P 而不是 Ctrl+K，是因为后者在终端里是「删到行尾」。
- **同步输入**。在会话栏点「同步」把这台加入同步组，组里有两台以上时，
  在任一台里敲的东西会同时发给其它台 —— 一批机器上做同一件事时用。
  真的在广播时按钮会变成警告色，终端上方还会压一条横幅写明发给几台，
  横幅上一点就能把这台退出去。这个组不会存下来，重开应用就散。
- **分屏**。标签上右键「在右边分屏打开」，两个标签并排看；再点一下可以改成上下分。
  中间那条线拖着能调宽窄，双击回到一半一半。左边跑着日志、右边敲命令这种活儿
  不用再来回切标签了。

- **持久会话（tmux / screen）**。连接配置里选上之后，shell 会跑在 tmux 里，
  断线时服务器那边正在跑的命令不会跟着没，重连接回的还是同一个会话。
  会话名按连接固定，所以「重连」真的是接回去而不是开一个新的。
  机器上没装 tmux 的话会说一句然后退回普通 shell，不会把终端撂在那儿。

- **加密导出**。「设置 → 数据」多了一个「加密导出」，用口令把备份文件封起来
  （Argon2id 派生密钥 + XChaCha20-Poly1305 加密）。备份里本来就没有密码，
  但主机名、用户名、跳板机链路、内网端口本身就是情报 —— 文件要过网盘或者聊天工具
  的话就用它。导入时会自己认出是加密的并问口令。**口令没有找回**。

- **容器面板**。状态页多一块「容器」：列出这台机器上所有 docker 容器（在跑的和停了的），
  能看最近 300 行日志，能启动 / 停止 / 重启 / 强杀，也能一键把
  `docker exec -it … sh` 送进终端进到容器里。docker 要 root 的机器上勾一下「用 sudo」。
- **端口一键转发**。端口表里那些「仅本机」的（3306、6379、内部面板这类）
  右边多了「转发到本地」，点一下就在本机开个口连过去，不用记下端口号再去隧道页手填。
  开着的本地转发还多了个「在浏览器里打开」。

- **网络工具**。状态页多一块「网络」：ping、端口探测、DNS 解析、路由追踪。
  意义不在于「有个图形界面」——在于这几条是**在那台服务器上跑的**。
  你本机 ping 得通不代表它 ping 得通，内网域名多半也只有它解得出来。
- **全局热键**（设置里选）。不在前台也能一下把窗口叫出来，再按一下收回去。
  组合被别的软件占了会明说，不会装作设好了。

- **从别的工具导入**。「设置 → 数据」里可以一次选多个文件，认 Xshell 的 `.xsh`、
  PuTTY 导出的 `.reg`、以及 Termius 这类工具导出的 JSON，把里面的服务器捞出来。
  只取主机、端口、用户名三样；**密码不认** —— 那几家的密码是用它们自己的密钥加密的，
  解得开也不该解。认出来之后先摆给你看，确认了才进库。

- **网盘同步（WebDAV）**。把加密后的备份放到自己的网盘上，换台机器拉回来。
  只有你点「上传」/「拉下来」的时候才发生 —— 没有自动同步、没有后台轮询。
  **传上去的永远是密文**，这一条不给开关。网盘的账号密码存系统钥匙串，不落配置文件。

- **命令补全建议**。敲到第三个字符起，如果这条会话里执行过以它打头的命令，
  终端底下会浮一条建议，按 `→` 或者点一下就补全。历史**只在内存里**，不落盘 ——
  命令行里带着密码是常事。
  跟丢了就不提示：一按方向键、Ctrl+U、Tab 或者粘贴一段东西，行内容就不再由我们说了算，
  那时候宁可什么都不给，也不能给一条错的。

### 变更

- **开始页**。标签栏空白处双击、Ctrl+Shift+N、或者一个标签都没有的时候，看到的是一页入口：
  连服务器、本机终端、数据库、备忘、命令面板、设置各一张卡，下面是最近连过的几台，点一下就开。
- **侧栏可以收起**。页脚多了一个收起按钮（也可以按 Ctrl+Shift+B），收起后只剩一条图标栏；
  点图标，那一栏的内容会以浮层弹在旁边，点外面、按 Esc、或者打开了什么标签就自动收回去。
  连接、指令、备忘三栏的内容和展开时是同一份，右键菜单、拖拽照常能用。
- **快捷键改版**。应用级的快捷键一律带 Shift 或 Alt，跟 shell 里的 Ctrl 组合井水不犯河水：
  以前 Ctrl+W / Ctrl+T / Ctrl+1..9 在终端里要么发不出去、要么抢了 bash 的删词和换字符，
  现在是 Ctrl+Shift+W 关标签、Ctrl+Shift+T 新建连接、Alt+1..9 跳标签，
  会话里的几页用 Ctrl+Shift+1..5 切，Ctrl+Shift+, 开设置；Ctrl+Tab 换标签不变。
  终端里新增 Ctrl+Shift+A 全选、Ctrl+Shift+K 清屏。
- **终端右键菜单**把常用的都收进来了，每条后面写着快捷键：复制粘贴、查找、全选、清屏，
  切到文件 / 状态 / 隧道 / 配置页，断开，命令面板，新建连接，关标签，设置。
  「设置」里也多了一页速查表。

### 说明

- 状态、进程、磁盘三样都**不需要在服务器上装任何东西**，走的是终端那条 SSH 连接上
  另开的子通道，读 `/proc` 和 `df` / `ps` / `du`，不二次认证、也不占用交互 shell。
- 目前只支持 Linux。macOS / BSD 上会明说「看不了」，而不是拿别处凑来的数画一张
  半真半假的图。
- 「登录后执行」那一栏是明文存在配置里的，别往里写密码；导出配置会带上它，
  导入别人的配置前先看一眼这一栏。

## 0.7.0 —— 2026-08-18

### 体验

- **新建连接只有主机必填**。用户名留空按 `root` 连，离开输入框时会把 `root` 填进去，
  存下去的是什么一眼看得见。
- **密码想什么时候填都行**。配置页里可以顺手填一条，也可以空着留到连接的时候再输；
  两处都带「连上之后记住」的勾，默认勾上，不会为这件事再弹一个框。
  填的密码在内存里交给那条会话，认证过了才写进系统钥匙串 —— 打错的密码不会被记住。
- **新建页分成「创建并连接」和「只创建」**，建完多半是要连的，就让它当主按钮。
- **侧栏单击落在连接卡**（原来落在配置页），配置改走 ✎ 按钮和右键菜单。
- **双击真的会连**。原来先单击开了标签、再双击只是切过去，「连接」那一下没人执行；
  右键菜单里的「连接」「打开文件面板」同样修好。
- 连接卡上加了抬头，写清这次要连哪台机器。
- **检查更新装完不再自动重启**，改成手动点「重启生效」，并提示现在有几条会话连着 ——
  用户点的可能只是「查一下」，不该顺手把在跑的会话掐掉。
- 「记住上次的标签」现在连**停在哪一页**（终端 / 文件 / 隧道 / 配置）也一起记住。
- 文件面板底部的提示（「记下 3 项」这类）不再画成红色错误；传输进行中出的错也不会
  被队列进度盖住了。
- 端口转发：改一条不再跳到列表末尾；删之前问一句。
- **关窗口前会问一句**：编辑器 / 配置页里还有没保存的改动，或者还有会话连着，
  直接关窗不再一声不响地全丢掉。没什么可丢的时候照常一下就关。
- 对外名字统一成 **AzTerm**：窗口标题、欢迎页、「关于」、导入导出对话框。
  安装包标识和二进制名不动 —— 改那些会牵动更新器匹配。

### 修复

- **文件面板：在没选中的那一行上操作，只作用于那一行**。原先选了若干项之后，点别的
  行的删除 / 传输按钮、或者右键它，操作的是「之前选中的那些」—— 删错文件的路子。
  现在跟资源管理器一致：这一行在选中里就是整批，不在就只有它自己。
- **删除连接时清掉别人对它的跳板机引用**。原来引用会悬空，那些本该先过跳板机的连接
  会闷声直连。同时清掉这条连接的收藏目录和「上次待的目录」记录。
- 连接配了跳板机、但那条连接已经不在了时，**停下来报错**，不再退化成直连。
- 导入备份时，用户名为空的连接不再被整条丢掉（按 `root` 处理）。
- 同一栏内移动目录时，`/a/bc` 不再被误判成在 `/a/b` 里面而拒绝。
- 弹窗里的换行现在真的换行。
- 传输队列「取消」的说明跟实际对上了：排队的丢掉，正在传的也会叫停。

## 0.6.0 —— 2026-08-18

首个公开版本。

- **SSH 终端**：xterm.js + Rust（russh）引擎，PTY、颜色、回滚查找、复制粘贴、
  配色六套、字号四档、命令分割线；密码 / 密钥 / 键盘交互（2FA、OTP）三种认证。
- **字符编码**：UTF-8 / GBK / Big5 / Shift_JIS 等，连着也能当场换。
- **端口转发**：本地转发（-L）、动态 SOCKS5（-D）、远程转发（-R），复用同一条已认证的连接。
- **跳板机**：相当于 ssh 的 ProxyJump。
- **SFTP 文件面板**：本地 / 远程双栏，多选、框选、方向键导航、目录整体传输、并发传输、
  断点续传、覆盖确认、目录同步、同栏内复制 / 移动、收藏目录、拖拽上传、改权限。
- **内置编辑器**：远程配置文件直接改，Ctrl+S 存回原路径；编码切换、改动冲突检测、
  保留原换行风格、查找替换、跳行、行号。
- **会话日志**、**掉线自动重连**、**指令库**（45 条预设）、**备忘录**、**多标签**。
- 配置一键导出成 JSON，也能从 `~/.ssh/config` 导入。
- 凭据存系统钥匙串，配置文件里没有明文；主机指纹用标准 `~/.ssh/known_hosts`。
- 自动更新带 minisign 签名校验；版本提示走自己的接口，安装包从 GitHub 下载。
